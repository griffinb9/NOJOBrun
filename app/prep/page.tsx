import MobilePrep from '@/components/mobile/MobilePrep';

/** Mobile-friendly prep job list (bottom nav stays visible via AppGate). */
export default function PrepIndexPage() {
  return <MobilePrep />;
}
