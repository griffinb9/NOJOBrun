# App Icon

Place a single `icon.png` here — **1024 × 1024 px**, no rounded corners, no transparency.

Capacitor's asset generation tool (`npx capacitor-assets generate`) will resize it into
every required Xcode asset-catalog size automatically.

Brand reference: blue-500 (#3B82F6) → violet-600 (#7C3AED) gradient on a stone-50 (#F8F7F5) background.
