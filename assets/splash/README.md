# Splash Screen

Place a single `splash.png` here — **2732 × 2732 px** (safe content in the center 1200 × 1200 px).

Capacitor's asset generation tool (`npx capacitor-assets generate`) will resize it for all
required device sizes automatically.
